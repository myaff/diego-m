include ../mixins
.header
  .l-container
    .header__content
      .header__logo.header__logo--elle
        a(href=data.links.elle, target="_blank")
          +icon('elle')
      .header__title !{data.main.title}